# command-line-animation
# Sleep Spinner Usage

### How to use it

Replace your old code with just **one line**:

```python
sleep_with_spinner()                    # 400 seconds (matches your original duration)
sleep_with_spinner(10.0)                # 10 seconds
sleep_with_spinner(30.0, "Waiting for API")   # custom message
